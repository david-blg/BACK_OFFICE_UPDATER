import SectionPage from "@/layouts/SectionPage"
import Stacks from "./Stacks"

const StackSection = () => {
  return (
    <SectionPage
      title="Stacks"
      content={
        <Stacks />
      } />
  )
}

export default StackSection